/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'ik.imagekit.io',
      'picsum.photos',
      "lh3.googleusercontent.com"
    ],
  },
};

export default nextConfig;
